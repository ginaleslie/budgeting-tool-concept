import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks"
import ColorPickerButton from "./"

<Meta title="360 Menu/ColorPickerButton" component={ColorPickerButton} />

# Default:

<Canvas>
  <Story name="default">
    <ColorPickerButton
      click={() => alert("Button clicked")}
      item="food"
      radioColor="red"
    ></ColorPickerButton>
  </Story>
</Canvas>
